{
  "name": "branch-changes",
  "displayName": "branch-changes",
  "description": "See all the file diffs between the current branch and a selected branch.",
  "publisher": "lterodde",
  "repository": {
    "type": "git",
    "url": "https://github.com/lterodde320/branch-changes.git"
  },
  "author": {
    "name": "Leandro Terodde",
    "email": "leandro@terodde.lu"
  },
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.98.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gitChanges",
          "title": "Git Changes",
          "icon": "resources/git.svg"
        }
      ]
    },
    "views": {
      "explorer": [
        {
          "id": "gitChangedFilesView",
          "name": "Changed Files"
        }
      ]
    },
    "commands": [
      {
        "command": "gitChangedFiles.refresh",
        "title": "Refresh Changed Files"
      },
      {
        "command": "branch-changes.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "gitChangedFiles.selectBranch",
        "title": "Select Comparison Branch",
        "icon": "$(git-branch)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "gitChangedFiles.selectBranch",
          "when": "view == gitChangedFilesView",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.98.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.25.0",
    "@typescript-eslint/parser": "^8.25.0",
    "eslint": "^9.21.0",
    "typescript": "^5.7.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  }
}
